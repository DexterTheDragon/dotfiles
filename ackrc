# Always sort the files
--sort-files

--pager=less -FRSX

# Always color, even if piping to a another program
# --color

# Add file types
--type-add
ruby=.haml

--type-set
cucumber=.feature
--type-add
cucumber=.scenario

--type-set
puppet=.pp

--type-add
php=.ctp

--ignore-dir=coverage
